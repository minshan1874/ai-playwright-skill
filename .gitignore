# ─────────────────────────────────────────────────────────────────────
# 依赖与运行时工具链
#
# scripts/bootstrap.mjs 会把 Playwright 依赖装到「运行目录」
# （默认 ~/.dsh/playwright-e2e/），正常情况下不会出现在本仓库里。
# 只有在把 PLAYWRIGHT_E2E_HOME 指向本仓库时才会落到这里。
# 注意：bootstrap 会拒绝在已有 package.json 的目录里安装，不会覆盖你的项目。
# ─────────────────────────────────────────────────────────────────────
node_modules/
.npm-cache/

# ─────────────────────────────────────────────────────────────────────
# 凭据 —— 绝对不要提交
# ─────────────────────────────────────────────────────────────────────

# 保存的登录态（storageState）里是真实会话 Cookie
auth/
*.storage-state.json

# 本地配置可能含账号密码。示例文件 *.example.json 仍然需要提交。
e2e.config.json

# 环境变量文件（凭据通常通过 E2E_USERNAME / E2E_PASSWORD 注入）
.env
.env.*
!.env.example

# ─────────────────────────────────────────────────────────────────────
# 运行产物
# ─────────────────────────────────────────────────────────────────────
runs/
playwright-report/
blob-report/
test-results/
playwright/.cache/

# ─────────────────────────────────────────────────────────────────────
# 本地验证沙箱（见 README「离线验证」）
# ─────────────────────────────────────────────────────────────────────
.e2e-verify/
.e2e-ci/
.playwright-e2e/
.playwright-browsers/
.pw-browsers/

# ─────────────────────────────────────────────────────────────────────
# 日志
# ─────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ─────────────────────────────────────────────────────────────────────
# 编辑器与 IDE
# ─────────────────────────────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.project
.classpath

# ─────────────────────────────────────────────────────────────────────
# 操作系统
# ─────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ─────────────────────────────────────────────────────────────────────
# 其它
# ─────────────────────────────────────────────────────────────────────
coverage/
*.tgz
*.tar.gz
